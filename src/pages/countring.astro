---
import Layout from "../layouts/Layout.astro";

import { getCollection } from "astro:content";

const countries = await getCollection("countries");
---

<Layout title="Countring">
	<h1>Countring</h1>

	{countries.map((country) => country.id).join(" ")}
</Layout>
